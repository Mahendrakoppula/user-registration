import React from 'react';
import css from './live.module.scss';

const LiveIcon = () => {
  return (
    <div className={css['container']}>
      <span className={css['live-icon']}></span>
    </div>
  );
};
export default LiveIcon;
